/* eslint semi: ["error", "always"] */
import TableInput from './table-input.vue';

/* istanbul ignore next */
TableInput.install = function (Vue) {
  Vue.component(TableInput.name, TableInput);
};

export default TableInput;
